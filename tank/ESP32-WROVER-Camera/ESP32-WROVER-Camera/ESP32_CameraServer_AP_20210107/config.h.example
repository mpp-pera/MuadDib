#pragma once

// Copy this file to config.h and fill in your values.

// Home router credentials for STA mode.
// If connection fails within the timeout, firmware falls back to AP mode.
#define HOME_SSID     "your_network_name"
#define HOME_PASSWORD "your_password"

// IoT hub server address
#define HUB_HOST "192.168.1.x"
#define HUB_PORT 8000
